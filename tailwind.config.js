/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./includes/**/*.{php,html,js}",
        "./assets/**/*.{php,html,js}",
    ],
    theme: {
        extend: {
            content: {
            },
            backgroundImage: {
            }
        }
    },
    plugins: [
    ]
}